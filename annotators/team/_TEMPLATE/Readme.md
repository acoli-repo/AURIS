# TEMPLATE: Add your name here

Every annotator should be identified by their initials, optionally complemented with their birth data (not year) if there is more than one individual with the same initials.

Data resides in one or more sub-directories identified by language, e.g., `de/` for German and `en/` for English. The template uses `de/`. Change as needed.
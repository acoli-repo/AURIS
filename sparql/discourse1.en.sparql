PREFIX conll: <http://ufal.mff.cuni.cz/conll2009-st/task-description.html#> 
PREFIX nif:   <http://persistence.uni-leipzig.org/nlp2rdf/ontologies/nif-core#> 
PREFIX powla: <http://purl.org/powla/powla.owl#> 
PREFIX rdf:   <http://www.w3.org/1999/02/22-rdf-syntax-ns#> 
PREFIX rdfs:  <http://www.w3.org/2000/01/rdf-schema#> 
PREFIX terms: <http://purl.org/acoli/open-ie/> 
PREFIX x:     <http://purl.org/acoli/conll-rdf/xml#> 
PREFIX ontolex: <http://www.w3.org/ns/lemon/ontolex#>

#######################
# find CORE predicate #
#######################
# The NUC of the sentence is to be automatically identified with the following rules:
# 
# 1. initial NUC candidate is the syntactic head according to UD

INSERT { ?s conll:NUC ?w } 
WHERE {  ?w conll:HEAD ?s. 
         ?s a nif:Sentence. 
};

DELETE { ?v conll:NUC ?s }
WHERE {  ?w conll:NUC ?s.
		 ?v conll:NUC ?s. FILTER(?s != ?t).
		 ?w conll:UPOS "VERB".
		 ?v conll:UPOS ?tpos FILTER(?tpos != "VERB")
};

DELETE { ?s conll:NUC ?v }
INSERT { ?s rdfs:comment ?msg }
WHERE {  ?w conll:NUC ?s.
		 ?v conll:NUC ?s. 
		 ?w nif:nextWord+ ?v.
		 ?v conll:WORD ?vWord; conll:ID ?vId.
		 BIND(concat("WARNING: multiple head candidates, dropping ",str(?vId)," ",?vWord) as ?msg)
};

# 2. if the current NUC candidate is an attribution verb, then
# 2.0 identify attribution verbs, pointing to the respective head
INSERT { ?w conll:ATTRIBUTION ?h }
WHERE {  ?w conll:HEAD ?h.
		 ?w conll:LEMMA ?wLemma.
		FILTER(?wLemma in ("accept", "acknowledge", "add", "address", "admit", "advise", "affirm", "agree", "allow", "analyze", "announce", "answer", "argue", "ask", "assert", "assume", "assure", "aver", "believe", "categorize", "challenge", "charge", "cheer", "cite", "claim", "clarify", "command", "comment", "communicate", "compare", "complain", "concede", "conclude", "concur", "confess", "confirm", "congratulate", "consider", "contend", "continue", "counter", "criticize", "cry", "decide", "declaim", "declare", "defend", "define", "demonstrate", "denie", "deny", "describe", "disagree", "disclose", "discourse", "discuss", "dispute", "echo", "emphasize", "ensure", "entail", "envision", "establish", "exclaim", "explain", "express", "fantasize", "find", "foresee", "foretold", "forth", "grant", "grieve", "hear", "help", "hold", "hypothesize", "illustrate", "imply", "indicate", "inform", "insert", "insist", "interpret", "introduce", "invoke", "judge", "justifi", "justify", "know", "lament", "laugh", "learn", "list", "listen", "maintain", "mention", "mock", "mourn", "mutter", "note", "notice", "object", "observe", "offer", "oppose", "out", "outline", "perceive", "persuade", "point", "pray", "preach", "predict", "promise", "pronounce", "propose", "propound", "question", "rage", "read", "realize", "reason", "recall", "recite", "recognize", "recommend", "refute", "reject", "remark", "remember", "remind", "repeat", "reply", "report", "request", "require", "respond", "reveal", "ridicule", "roar", "say", "see", "seem", "set", "shout", "show", "shrug", "sing", "speak", "specify", "speculate", "spoke", "state", "struck", "suggest", "support", "suppose", "swear", "swore", "talk", "teach", "tell", "think", "threat", "threaten", "translate", "triumph", "try", "uncovered", "understand", "understood", "upheld", "uphold", "urge", "use", "utter", "verify", "view", "want", "warn", "watch", "weep", "wish", "with", "wonder", "write"))
};

# 2.a set the NUC candidate to the first dependent non-attribution verb
DELETE { ?s conll:NUC ?v }
INSERT { ?s conll:NUC ?w }
WHERE { ?s conll:NUC ?v.
		?w conll:ATTRIBUTION+ ?v. 
		?w conll:UPOS "VERB".
	    MINUS { ?w conll:ATTRIBUTION [] }
};

# 2.b if this is not possible, set the NUC candidate to the first direct dependent with clausal dependents
DELETE { ?s conll:NUC ?v }
INSERT { ?s conll:NUC ?w }
WHERE { ?s conll:NUC ?v.
		?v conll:ATTRIBUTION [].
		?w conll:ATTRIBUTION+ ?v. 
		MINUS { ?w conll:ATTRIBUTION [] }
		?dep conll:HEAD ?w.
		?dep conll:EDGE ?depEdge.
		FILTER(replace(?depEdge,":.*","") in ("aux","nsubj","csubj","obj","xcomp","advcl"))
};

# 2.c if this is not possible, set the NUC candidate to its first csubj, ccomp or xcomp argument
DELETE { ?s conll:NUC ?v }
INSERT { ?s conll:NUC ?w }
WHERE { ?s conll:NUC ?v.
		?v conll:ATTRIBUTION [].
		?w conll:ATTRIBUTION+ ?v. 
		MINUS { ?w conll:ATTRIBUTION [] }
		?w conll:EDGE ?wEdge.
		FILTER(replace(?wEdge,":.*","") in ("csubj","ccomp","xcomp"))
};

# 2.d if this is not possible, keep the lowest NUC candidate
DELETE { ?s conll:NUC ?v }
INSERT { ?s conll:NUC ?w }
WHERE { ?s conll:NUC ?v.
		?v conll:ATTRIBUTION [].
		?w conll:ATTRIBUTION+ ?v. 
		MINUS { [] conll:ATTRIBUTION ?w }
};

# 2.e keep the first NUC candidate
DELETE { ?s conll:NUC ?w }
WHERE { ?s conll:NUC ?v,?w.
	    ?v nif:nextWord+ ?w.
};

# 2.f remove ATTRIBUTION
DELETE { ?x conll:ATTRIBUTION ?y }
WHERE {  ?x conll:ATTRIBUTION ?y };

# 3. if the current NUC candidate is a nominal, adjectival or adverbial
#    predicate of a copula clause with an explicit verb, make the copular verb the NUC candidate.

DELETE { ?s conll:NUC ?v }
INSERT { ?s conll:NUC ?w }
WHERE {  ?s conll:NUC ?v. 
		 ?w conll:HEAD ?v. 
		 ?w conll:EDGE ?wEdge. FILTER(strstarts(?wEdge,"cop"))
};

# 4. create conll:PRED attribute from conll:NUC
INSERT { ?s conll:PRED ?pred_formatted }
WHERE { ?s conll:NUC ?p .
		{ SELECT ?p (GROUP_CONCAT(?lemma; separator=" ") AS ?pred)
		  WHERE { ?s conll:NUC ?p. ?p conll:HEAD*/conll:LEMMA ?lemma }
		  GROUP BY ?p
		}
		?p conll:LEMMA ?pLemma.
		BIND(if(?pLemma=?pred, 
		        ?pLemma, 
		        concat(?pLemma," (",?pred,")")) 
		     as ?pred_formatted)
};
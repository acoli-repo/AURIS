# logs

In general, annotation logs should be stored together with the annotations, in a file with the same name.

some scattered remarks are collected here:

- `is/en/jc`: 700 tok / 65 min (doyle,  `COREF`, `REF`, `IS`, no `CB`)
- `is/en/fb`: 1000 tok / 60 min (doyle,  `COREF`, `REF`, `IS`, no `CB`)